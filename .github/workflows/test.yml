name: CI - clonando App y ejecutando tests

on: 
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      mongo:
        image: mongo:6.0
        ports:
          - 27017:27017
        
    env:
      MONGO_URI: ${{ secrets.MONGO_URI }}
      JWT_SECRET: ${{ secrets.JWT_SECRET }}
      PORT: 6007
      VITE_API_URL: http://localhost:6007/api
 
    steps:
    - name: Checkout repo actual (con el workflow)
      uses: actions/checkout@v3

    - name: Crear archivos .env para el backend
      run: |
        # Buscamos la carpeta 'backend' sin importar dónde esté parada
        BACKEND_DIR=$(find . -name "backend" -type d | head -n 1)
        echo "MONGO_URI=${{ secrets.MONGO_URI }}" >> $BACKEND_DIR/.env
        echo "JWT_SECRET=${{ secrets.JWT_SECRET }}" >> $BACKEND_DIR/.env
        echo "PORT=6007" >> $BACKEND_DIR/.env

    - name: Instalar dependencias de backend
      run: |
        cd redux-athena-bank/backend
        npm install

    - name: Construir backend
      run: |
        cd redux-athena-bank/backend
        # Usamos node directo para evitar problemas de permisos de npm
        nohup node src/server.js > backend.log 2>&1 &
        sleep 20

    - name: Instalar wait-on
      run: npm install -g wait-on

    - name: Esperar a que el backend esté disponible
      run: npx wait-on tcp:6007 --timeout 60000 # Máximo 1 minuto

    - name: Ver logs si el backend falla
      if: failure()
      run: cat redux-athena-bank/backend/backend.log

    - name: Instalar dependencias del frontend
      run: |
        cd redux-athena-bank/frontend
        npm install

    - name: Esperar a que el frontend esté disponible
      run: wait-on http://localhost:3000
    - name: Ejecutar tests 
      run: |
        cd redux-athena-bank/frontend
        npm run test
